<?xml version="1.0" encoding="UTF-8"?>
<Wix xmlns="http://schemas.microsoft.com/wix/2006/wi">
  <Product Id="*" 
           Name="Visco Connect Demo build v2.1.5" 
           Language="1033" 
           Version="2.1.5.0" 
           Manufacturer="Visco Technologies" 
           UpgradeCode="A1B2C3D4-E5F6-7890-ABCD-123456789ABC">
    
    <Package InstallerVersion="200" 
             Compressed="yes" 
             InstallScope="perMachine" 
             Platform="x64"
             Description="Visco Connect Demo build v2.1.5 - IP Camera Port Forwarding Application (Demo Version)" />

    <MajorUpgrade DowngradeErrorMessage="A newer version of [ProductName] is already installed." />
    <MediaTemplate EmbedCab="yes" />

    <!-- Feature definition -->
    <Feature Id="ProductFeature" Title="Visco Connect Demo build v2.1.5 Application" Level="1">
      <ComponentGroupRef Id="HarvestedFiles" />
      <ComponentRef Id="DesktopShortcut" />
      <ComponentRef Id="ProgramMenuShortcuts" />
    </Feature>

    <!-- Directory structure -->
    <Directory Id="TARGETDIR" Name="SourceDir">
      <Directory Id="ProgramFiles64Folder">
        <Directory Id="INSTALLFOLDER" Name="Visco Connect Demo" />
      </Directory>
      <Directory Id="DesktopFolder" Name="Desktop" />
      <Directory Id="ProgramMenuFolder" Name="Programs">
        <Directory Id="ApplicationProgramsFolder" Name="Visco Connect Demo" />
      </Directory>
    </Directory>

    <!-- Desktop shortcut -->
    <Component Id="DesktopShortcut" Directory="DesktopFolder" Guid="A1B2C3D4-E5F6-7890-ABCD-111111111111">
      <Shortcut Id="DesktopShortcut"
                Name="Visco Connect Demo"
                Description="Visco Connect Demo build v2.1.5 - IP Camera Port Forwarding (Demo Version)"
                Target="[INSTALLFOLDER]ViscoConnect.exe"
                WorkingDirectory="INSTALLFOLDER" />
      <RemoveFolder Id="DesktopFolder" On="uninstall" />
      <RegistryValue Root="HKCU" 
                     Key="Software\ViscoConnectDemo" 
                     Name="DesktopShortcut" 
                     Type="integer" 
                     Value="1" 
                     KeyPath="yes" />
    </Component>

    <!-- Program menu shortcuts -->
    <Component Id="ProgramMenuShortcuts" Directory="ApplicationProgramsFolder" Guid="A1B2C3D4-E5F6-7890-ABCD-222222222222">
      <Shortcut Id="ApplicationStartMenuShortcut"
                Name="Visco Connect Demo"
                Description="Visco Connect Demo build v2.1.5 - IP Camera Port Forwarding (Demo Version)"
                Target="[INSTALLFOLDER]ViscoConnect.exe"
                WorkingDirectory="INSTALLFOLDER" />
      <Shortcut Id="UninstallProduct"
                Name="Uninstall Visco Connect Demo"
                Target="[SystemFolder]msiexec.exe"
                Arguments="/x [ProductCode]"
                Description="Uninstalls Visco Connect Demo" />
      <RemoveFolder Id="ApplicationProgramsFolder" On="uninstall" />
      <RegistryValue Root="HKCU" 
                     Key="Software\ViscoConnectDemo" 
                     Name="ProgramMenuShortcut" 
                     Type="integer" 
                     Value="1" 
                     KeyPath="yes" />
    </Component>

    <!-- UI for installation -->
    <UIRef Id="WixUI_InstallDir" />
    <Property Id="WIXUI_INSTALLDIR" Value="INSTALLFOLDER" />
    <WixVariable Id="WixUILicenseRtf" Value="$(var.SourceDir)\installer\License.rtf" />

    <!-- Custom UI message about firewall -->
    <UI>
      <Dialog Id="FirewallInfoDlg" Width="370" Height="270" Title="Firewall Configuration">
        <Control Id="Title" Type="Text" X="15" Y="6" Width="200" Height="15" Transparent="yes" NoPrefix="yes" Text="{\WixUI_Font_Title}Firewall Configuration" />
        <Control Id="Description" Type="Text" X="25" Y="23" Width="320" Height="80" Transparent="yes" NoPrefix="yes" Text="Visco Connect Demo requires specific Windows Firewall rules for proper operation:&#xD;&#xA;&#xD;&#xA;• TCP port 7777 - Echo Server functionality&#xD;&#xA;• ICMP Echo Request - Ping response capability&#xD;&#xA;&#xD;&#xA;These rules will be configured automatically when you run the application. The application requires Administrator privileges to manage firewall settings." />
        <Control Id="AdminNote" Type="Text" X="25" Y="110" Width="320" Height="40" Transparent="yes" NoPrefix="yes" Text="Note: When you first run Visco Connect Demo, Windows may prompt for Administrator permission to configure firewall rules. This is normal and required for proper functionality." />
        <Control Id="Back" Type="PushButton" X="180" Y="243" Width="56" Height="17" Text="&amp;Back">
          <Publish Event="NewDialog" Value="InstallDirDlg">1</Publish>
        </Control>
        <Control Id="Next" Type="PushButton" X="236" Y="243" Width="56" Height="17" Default="yes" Text="&amp;Next">
          <Publish Event="NewDialog" Value="VerifyReadyDlg">1</Publish>
        </Control>
        <Control Id="Cancel" Type="PushButton" X="304" Y="243" Width="56" Height="17" Cancel="yes" Text="Cancel">
          <Publish Event="SpawnDialog" Value="CancelDlg">1</Publish>
        </Control>
      </Dialog>

      <InstallUISequence>
        <Show Dialog="FirewallInfoDlg" Before="VerifyReadyDlg">NOT Installed</Show>
      </InstallUISequence>
    </UI>

  </Product>
</Wix>
